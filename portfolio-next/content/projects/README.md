# Projects Content (stub)

This folder will contain project entries used to statically generate the Projects listing and individual project pages.

Planned format:
- One folder or Markdown/JSON per project with minimal frontmatter (title, slug, summary).
- Assets (images, models) referenced from `/public`.

Nothing here yet. Content will be added in a later task.