# Writing Content (stub)

This folder will contain writing entries used to statically generate the Writing section.

Planned format:
- Markdown with minimal frontmatter (title, slug, summary).
- Assets referenced from `/public`.

Nothing here yet. Content will be added in a later task.